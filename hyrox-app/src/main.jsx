import React from "react";
import ReactDOM from "react-dom/client";
import FitnessApp from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <FitnessApp />
  </React.StrictMode>
);
